1）程序源码中的底层驱动都是参考教材附录B或者相关芯片出现的页数写的，只有温度传感器的底层驱动时参考博客写的。
2）程序源码中存放了整个工程可以直接使用，另外把main函数单独复制出来了一份方便查看，这个main函数对调试时的
一些注释代码做了删除并且做了点格式的美化，而工程中的main则保留了。
因为代码行数太多了，，没办法贴到word里，代码质量不太够。。
3）程序中最关键的函数是  display_deal();与key_deal()，这俩个完成了最主要的控制部分，设计思路就是按照流程图设计的，
但大都很多简单的判断和重复分解代码（如发送变量给LCD，就需要分解），也因为这两个函数让总体的代码数量增多。




